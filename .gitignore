# Secrets — do not commit
credentials.json
zoom_config.json
.streamlit/secrets.toml

# Attendance exports — participant names and emails, never commit these
*.csv

# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
venv/
.env
.env.*
